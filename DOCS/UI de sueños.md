OK 1 menos es mas.



STATUS
Detectar que el aparato USB controller mini se detecta correctamente.


MODOS

Modo lectura (muestra que hace cada boton al seleccionarlo, atajos delays macros etc) 


Modo configuracion (permite hacer cambios y guardarlos asi como guardar presets).

MODO CONFIGURACION
FLUJO:
 1 - Seleccionar el boton que quiero configurar.
 2 - Opcion Manual (escribir el atajo a mano ejemplo: "CTRL + C" (campo para escribir) y detection automatica, boton de detectar el usuario aplica el teclado se detecta). \*\*\*Aqui es muy importante definir configuracion del tipo de teclado e idioma, porque diferentes configuraciones daran diferentes resultados).
 3 - para el Knob tiene 3 funciones adelante atras y click, igual mono manual o modo automatic.
 4 - LEDs el controlador tiene leds no se como funcionan pero seria perfecto que fuean responsivos, por ejemplo que prendan cuando se le toca nada mas o que se queden prendidos del color o combinacon que yo defina, etc.
 4- Save, queda todo lo que se seleccionó se guarda.


